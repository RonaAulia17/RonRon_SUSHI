<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ronronsushi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sushiicon"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RonRonSUSHI">
        <activity android:name=".MainActivity"
            android:label="RonRon SUSHI!">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OutletActivity"
            android:label="RonRon SUSHI!">
           </activity>
        <activity android:name=".DaftarMenuActivity"
            android:label="RonRon SUSHI!">
        </activity>
        <activity android:name=".OrderActivity"
            android:label="RonRon SUSHI!"></activity>

    </application>

</manifest>